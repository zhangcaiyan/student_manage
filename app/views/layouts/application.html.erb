<!DOCTYPE html>
<html>
<head>
  <title>评优评助系统</title>
  <%= render "/shared/head" %>
</head>
<body>
  <% if user_signed_in? %>
    <%= link_to '退出', destroy_user_session_path, method: :delete %>
  <% end %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>

  <%= render "/shared/js" %>
</body>
</html>
